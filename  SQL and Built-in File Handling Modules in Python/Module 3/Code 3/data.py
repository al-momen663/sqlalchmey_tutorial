import config 
print(config.username)
print(config.password)

# this type of import and viewing is not recommended
# because it exposes sensitive information
# instead, use a config file or environment variables
# to store sensitive information
# and load them securely
